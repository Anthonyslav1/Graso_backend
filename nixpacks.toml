[phases.build]
commands = [
  "curl https://sh.rustup.rs -sSf | sh -s -- -y",   # Install Rust using rustup
  "source $HOME/.cargo/env",                        # Load Rust environment
  "pip install -r requirements.txt",                # Install Python dependencies
  "maturin develop"                                 # Build Rust extension
]
